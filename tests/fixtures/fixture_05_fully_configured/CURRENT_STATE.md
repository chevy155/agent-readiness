## Current State

Invoice service baseline is stable. CI, tests, and governance files are healthy.
Pending tasks should stay within `src/` and `tests/` unless operator approval expands scope.

## Next Step

Run the readiness scan and keep score GREEN before autonomous edits.
